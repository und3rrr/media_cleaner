# 📋 Summary: Серверная часть Media Cleaner

## Что было создано

Я создал полнофункциональную **серверную архитектуру** для Media Cleaner, которая позволяет:

✅ **Нескольким пользователям** одновременно загружать видео для обработки
✅ **Обрабатывать видео параллельно** в фоновом режиме
✅ **Отслеживать статус** каждой задачи
✅ **Управлять файлами** с уникальными ID для предотвращения конфликтов
✅ **Масштабировать** от 1 до N обработчиков

---

## 🗂️ Созданные файлы

### 1. **server_config.py** (конфигурация)
Централизованные настройки сервера:
- Пути к папкам (input, output, temp, logs)
- Параметры обработки по умолчанию
- Лимиты на размер видео и одновременные задачи
- Конфигурация логирования

### 2. **queue_processor.py** (очередь задач)
Система управления задачами:
- Класс `ProcessingTask` для хранения информации о задаче
- Класс `VideoProcessingQueue` для управления очередью
- Сохранение/загрузка задач в JSON БД
- Статусы: pending → processing → completed/failed

### 3. **server_app.py** (REST API)
FastAPI сервер с эндпоинтами:
- `POST /upload` - загрузить видео
- `GET /task/{task_id}` - статус задачи
- `GET /tasks` - список задач
- `GET /download/{task_id}` - скачать результат
- `POST /cancel/{task_id}` - отменить задачу
- `GET /stats` - статистика сервера
- `GET /health` - проверка здоровья

### 4. **server_video_worker.py** (обработка в фоне)
Фоновые потоки для обработки видео:
- Функция `process_video_task()` - обработка одного видео
- Функция `start_queue_processor()` - запуск N потоков
- Интеграция с основным модулем `media_cleaner.py`

### 5. **run_server.py** (запуск)
Главный скрипт:
- Парсинг аргументов командной строки
- Валидация конфигурации
- Запуск REST API и обработчиков очереди

### 6. **client.py** (клиент)
Python клиент для взаимодействия с сервером:
- Загрузка видео
- Проверка статуса
- Ожидание завершения
- Скачивание результатов
- Командная строка интерфейс

---

## 📚 Документация

### Для быстрого старта:
- **[QUICKSTART_SERVER.md](./QUICKSTART_SERVER.md)** - 5 минут до запуска

### Для администраторов:
- **[SERVER_DEPLOYMENT.md](./SERVER_DEPLOYMENT.md)** - развертывание на production
- **[DEPLOYMENT_CHECKLIST.md](./DEPLOYMENT_CHECKLIST.md)** - чеклист развертывания

### Для разработчиков:
- **[SERVER_ARCHITECTURE.md](./SERVER_ARCHITECTURE.md)** - как работает система
- **[INTEGRATION_EXAMPLES.md](./INTEGRATION_EXAMPLES.md)** - примеры кода

### Главный файл:
- **[SERVER_README.md](./SERVER_README.md)** - полный индекс документации

---

## 🏗️ Архитектура

```
Пользователь 1 ──┐
Пользователь 2 ──┤
Пользователь N ──┼──→ REST API (FastAPI) ──→ Очередь задач ──→ Обработчик 1
                 │                                          ├→ Обработчик 2
                 └──→ Выходные файлы                       └→ Обработчик 3
```

### Поток работы:

1. **Загрузка** - пользователь загружает видео через `/upload`
2. **Уникализация** - присваивается уникальный `task_id`
3. **В очередь** - видео добавляется в очередь обработки
4. **Обработка** - фоновый поток обрабатывает видео
5. **Результат** - готовое видео сохраняется в `videos_output/`
6. **Скачивание** - пользователь скачивает через `/download/{task_id}`

---

## 🚀 Быстрый старт

### Установка и запуск (2 команды):

```bash
pip install -r requirements.txt
python run_server.py
```

### Открыть в браузере:

```
http://localhost:8000/docs
```

### Загрузить видео (Python):

```python
from client import MediaCleanerClient

client = MediaCleanerClient("http://localhost:8000")
result = client.upload_video("video.mp4", user_id="user_001")
task = client.wait_for_completion(result['task_id'])
client.download_result(task['task_id'], output_path="./results")
```

### Загрузить видео (командная строка):

```bash
python client.py upload video.mp4 --wait --download ./results
```

---

## 🔑 Ключевые особенности

### ✅ Уникализация файлов

Каждому видео присваивается уникальный ID:
```
task_id = "a1b2c3d4"
input:  "a1b2c3d4_video.mp4"
output: "a1b2c3d4_video_protected.mp4"
```

Это предотвращает конфликты при одновременной обработке видео с одинаковыми именами.

### ✅ Управление очередью

Система задач поддерживает статусы:
- `pending` - ожидает обработки
- `processing` - обрабатывается
- `completed` - успешно обработано
- `failed` - ошибка при обработке
- `cancelled` - отменено пользователем

### ✅ Параллельная обработка

```bash
python run_server.py --workers 3  # 3 видео одновременно
```

### ✅ Фоновая обработка

API немедленно возвращает `task_id`, а обработка идёт в фоне.

### ✅ Отслеживание прогресса

```python
task = client.get_task_status(task_id)
print(f"Прогресс: {task['progress']}%")
```

---

## 📊 Структура папок

Сервер автоматически создаёт и использует:

```
media_cleaner/
├── videos_input/      # Входящие видео (удаляются после обработки)
├── videos_output/     # Готовые видео (скачиваются пользователями)
├── videos_temp/       # Временные файлы (удаляются автоматически)
├── server_logs/       # Логи сервера (для отладки)
├── queue_db/          # БД задач в JSON (tasks.json)
└── __pycache__/       # Кэш Python
```

---

## 🔐 Безопасность для multi-user окружения

1. **Уникальные ID** - каждый файл имеет уникальный ID
2. **Изоляция пользователей** - можно добавить проверку `user_id`
3. **Лимиты** - макс размер видео, макс одновременные задачи
4. **Логирование** - все операции логируются для аудита

---

## 💻 Примеры использования

### Python (рекомендуется)
```python
from client import MediaCleanerClient
client = MediaCleanerClient("http://localhost:8000")
client.upload_video("video.mp4")
```

### Командная строка
```bash
python client.py upload video.mp4 --wait --download ./results
```

### Web браузер
```
http://localhost:8000/docs
```

### curl
```bash
curl -X POST "http://localhost:8000/upload" \
  -F "file=@video.mp4" \
  -F "epsilon=0.15"
```

### Flask/Django веб приложение
```python
# См. INTEGRATION_EXAMPLES.md для полного примера
result = client.upload_video(file_path)
return {"task_id": result['task_id']}
```

---

## 📈 Масштабирование

### Одиночный сервер
```bash
python run_server.py --workers 4
```

### Несколько серверов (load balancing)
```
Nginx (порт 80)
  ├→ Server 1 (порт 8000)
  ├→ Server 2 (порт 8000)
  └→ Server 3 (порт 8000)
```

### Docker контейнер
```bash
docker build -t media-cleaner .
docker run -p 8000:8000 media-cleaner
```

---

## 📝 REST API Эндпоинты

| Метод | Путь | Описание |
|-------|------|---------|
| POST | `/upload` | Загрузить видео |
| GET | `/task/{id}` | Статус задачи |
| GET | `/tasks` | Список задач |
| GET | `/download/{id}` | Скачать видео |
| POST | `/cancel/{id}` | Отменить задачу |
| GET | `/stats` | Статистика |
| GET | `/health` | Проверка здоровья |

Интерактивная документация: **http://localhost:8000/docs**

---

## ⚙️ Параметры обработки

| Параметр | Диапазон | По умолчанию | Описание |
|----------|----------|--------------|---------|
| epsilon | 0.04-0.20 | 0.12 | Сила видео-шума |
| video_strength | 1.0-2.0 | 1.0 | Множитель |
| audio_level | None/"слабый"/"средний"/"сильный" | "слабый" | Маскировка аудио |
| every_n_frames | 1-30 | 10 | Каждый N-й кадр |

---

## 🐛 Трубешутинг

### "Сервер не запускается"
```bash
python server_config.py  # Проверить конфигурацию
```

### "Видео не обрабатывается"
```bash
tail -f server_logs/queue.log  # Посмотреть ошибку
```

### "Медленно обрабатывается"
```bash
python run_server.py --workers 8  # Больше обработчиков
```

---

## 📚 Документация

| Файл | Для кого | Время чтения |
|------|----------|--------------|
| [QUICKSTART_SERVER.md](./QUICKSTART_SERVER.md) | Всем | 5 минут |
| [SERVER_README.md](./SERVER_README.md) | Администраторы | 10 минут |
| [SERVER_DEPLOYMENT.md](./SERVER_DEPLOYMENT.md) | DevOps | 30 минут |
| [SERVER_ARCHITECTURE.md](./SERVER_ARCHITECTURE.md) | Разработчики | 20 минут |
| [INTEGRATION_EXAMPLES.md](./INTEGRATION_EXAMPLES.md) | Разработчики | 30 минут |
| [DEPLOYMENT_CHECKLIST.md](./DEPLOYMENT_CHECKLIST.md) | DevOps | По мере выполнения |

---

## 🎯 Следующие шаги

1. **Быстрый старт** → прочитайте [QUICKSTART_SERVER.md](./QUICKSTART_SERVER.md)
2. **Запустите сервер** → `python run_server.py`
3. **Протестируйте** → `python client.py health`
4. **Загрузите видео** → `python client.py upload video.mp4 --wait`
5. **Для production** → используйте [DEPLOYMENT_CHECKLIST.md](./DEPLOYMENT_CHECKLIST.md)

---

## ✨ Резюме

Теперь ваша программа Media Cleaner может:

- 🌐 Служить REST API для множества пользователей
- 📤 Принимать видео от разных источников
- 🔄 Обрабатывать их параллельно в фоне
- 💾 Управлять файлами с уникальными ID
- 📊 Отслеживать статус каждой задачи
- 🚀 Масштабироваться на сотни пользователей
- 🔒 Изолировать данные пользователей

**Всё готово для production использования!** 🎉

---

**Стартовой точкой является:** `python run_server.py` 🚀

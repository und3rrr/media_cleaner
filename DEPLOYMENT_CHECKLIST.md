# ✅ Чеклист развертывания Media Cleaner Server

## Фаза 1: Подготовка (~ 15 минут)

- [ ] Установить Python 3.8+ 
- [ ] Клонировать/скопировать проект Media Cleaner
- [ ] Перейти в папку проекта: `cd media_cleaner`
- [ ] Создать виртуальное окружение: `python -m venv venv`
- [ ] Активировать окружение:
  - Windows: `venv\Scripts\activate`
  - Linux/Mac: `source venv/bin/activate`
- [ ] Обновить pip: `pip install --upgrade pip`
- [ ] Установить зависимости: `pip install -r requirements.txt`

## Фаза 2: Конфигурация (~ 10 минут)

- [ ] Проверить конфигурацию: `python server_config.py`
  - Должно вывести: `✅ Конфигурация валидна`
  - Автоматически созданы папки:
    - [ ] videos_input/
    - [ ] videos_output/
    - [ ] videos_temp/
    - [ ] server_logs/
    - [ ] queue_db/

- [ ] Отредактировать `server_config.py` если нужно:
  - [ ] Изменить `host` (по умолчанию: 0.0.0.0)
  - [ ] Изменить `port` (по умолчанию: 8000)
  - [ ] Изменить `max_concurrent_tasks` (по умолчанию: 3)
  - [ ] Изменить `max_video_size_gb` (по умолчанию: 2)

## Фаза 3: Тестирование (~ 20 минут)

### 3.1 Запуск сервера
- [ ] Запустить сервер: `python run_server.py`
- [ ] Должны появиться логи:
  ```
  🚀 Запуск сервера обработки видео...
  ✓ Запущен обработчик #0
  🌐 API будет доступен по адресу: http://0.0.0.0:8000
  ```

### 3.2 Проверка API
- [ ] Открыть в браузере: `http://localhost:8000/docs`
- [ ] Должна загрузиться интерактивная Swagger документация
- [ ] Проверить эндпоинты:
  - [ ] GET `/` - основная информация
  - [ ] GET `/health` - проверка здоровья
  - [ ] GET `/stats` - статистика

### 3.3 Тестовая загрузка видео
- [ ] Использовать Python клиент:
  ```bash
  python client.py health
  ```
  Ожидаемый результат: `✅ Сервер здоров`

- [ ] Загрузить тестовое видео:
  ```bash
  python client.py upload test_video.mp4 --user test_user --wait --download ./results
  ```
  
- [ ] Проверить результаты:
  - [ ] Видео загружено в `videos_input/`
  - [ ] Создана задача в `queue_db/tasks.json`
  - [ ] Обработанное видео в `videos_output/`
  - [ ] Результат скачан в `./results/`

### 3.4 Проверка логирования
- [ ] Посмотреть основной лог:
  ```bash
  tail -f server_logs/server.log
  ```
- [ ] Посмотреть лог обработки:
  ```bash
  tail -f server_logs/queue.log
  ```
- [ ] Убедиться, что нет ошибок

## Фаза 4: Production развертывание (опционально)

### 4.1 Linux/Ubuntu (systemd сервис)

- [ ] Создать пользователя сервиса:
  ```bash
  sudo useradd -m media-cleaner
  ```

- [ ] Скопировать проект в `/home/media-cleaner/media_cleaner`

- [ ] Создать systemd сервис `/etc/systemd/system/media-cleaner.service`:
  ```ini
  [Unit]
  Description=Media Cleaner Video Server
  After=network.target

  [Service]
  Type=notify
  User=media-cleaner
  WorkingDirectory=/home/media-cleaner/media_cleaner
  Environment="PATH=/home/media-cleaner/media_cleaner/venv/bin"
  ExecStart=/home/media-cleaner/media_cleaner/venv/bin/python run_server.py --host 0.0.0.0 --workers 3
  Restart=always
  RestartSec=10

  [Install]
  WantedBy=multi-user.target
  ```

- [ ] Включить сервис:
  ```bash
  sudo systemctl daemon-reload
  sudo systemctl enable media-cleaner
  sudo systemctl start media-cleaner
  ```

- [ ] Проверить статус:
  ```bash
  sudo systemctl status media-cleaner
  ```

### 4.2 Nginx (обратный прокси)

- [ ] Установить Nginx:
  ```bash
  sudo apt-get install nginx
  ```

- [ ] Создать конфигурацию `/etc/nginx/sites-available/media-cleaner`:
  ```nginx
  server {
      listen 80;
      server_name your-domain.com;

      # Увеличиваем лимит размера загрузки
      client_max_body_size 2G;

      location / {
          proxy_pass http://127.0.0.1:8000;
          proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
          
          # Таймауты для больших видео
          proxy_connect_timeout 60s;
          proxy_send_timeout 300s;
          proxy_read_timeout 300s;
      }
  }
  ```

- [ ] Включить сайт:
  ```bash
  sudo ln -s /etc/nginx/sites-available/media-cleaner /etc/nginx/sites-enabled/
  sudo systemctl reload nginx
  ```

- [ ] Проверить конфигурацию:
  ```bash
  sudo nginx -t
  ```

### 4.3 SSL/HTTPS (опционально)

- [ ] Установить certbot:
  ```bash
  sudo apt-get install certbot python3-certbot-nginx
  ```

- [ ] Получить сертификат:
  ```bash
  sudo certbot --nginx -d your-domain.com
  ```

- [ ] Проверить автоматическое обновление:
  ```bash
  sudo systemctl enable certbot.timer
  ```

## Фаза 5: Проверка безопасности

- [ ] Ограничить доступ в файерволле:
  ```bash
  sudo ufw allow 80/tcp
  sudo ufw allow 443/tcp
  sudo ufw enable
  ```

- [ ] Установить лимиты на размер видео в `server_config.py`:
  - [ ] `max_video_size_gb`: 2GB
  - [ ] `max_concurrent_tasks`: 3

- [ ] Добавить аутентификацию (опционально):
  - [ ] API ключи в `server_app.py`
  - [ ] Проверка `user_id` при скачивании

- [ ] Настроить очистку старых файлов:
  ```python
  # server_config.py
  "auto_cleanup_days": 7,
  "cleanup_schedule_hour": 2,  # 2 часа ночи
  ```

## Фаза 6: Мониторинг и обслуживание

### 6.1 Ежедневные проверки
- [ ] Проверить статус сервиса:
  ```bash
  systemctl status media-cleaner
  ```

- [ ] Проверить логи на ошибки:
  ```bash
  grep ERROR server_logs/*.log
  ```

- [ ] Проверить свободное место на диске:
  ```bash
  df -h videos_output/
  ```

### 6.2 Еженедельно
- [ ] Проверить статистику:
  ```bash
  python client.py stats
  ```

- [ ] Очистить старые задачи:
  ```bash
  python client.py cleanup --days 7
  ```

- [ ] Резервирование БД:
  ```bash
  cp queue_db/tasks.json backups/tasks_$(date +%Y%m%d).json
  ```

### 6.3 Ежемесячно
- [ ] Проверить лог-файлы на размер
- [ ] Архивировать старые видео
- [ ] Обновить зависимости (если нужно)

## Фаза 7: Документация и интеграция

- [ ] Прочитать основную документацию:
  - [ ] [SERVER_README.md](./SERVER_README.md)
  - [ ] [QUICKSTART_SERVER.md](./QUICKSTART_SERVER.md)

- [ ] Выбрать метод интеграции:
  - [ ] Python клиент (client.py)
  - [ ] REST API (curl или requests)
  - [ ] Web интерфейс (HTML/JS)
  - [ ] Flask/Django приложение

- [ ] Создать примеры для команды:
  - [ ] Пример на Python
  - [ ] Пример через curl
  - [ ] Документация для конечных пользователей

## Фаза 8: Обучение команды

- [ ] Показать работу сервера:
  ```bash
  python client.py upload sample.mp4 --wait --download ./results
  ```

- [ ] Объяснить структуру:
  - [ ] Папка `videos_input/` - входящие видео
  - [ ] Папка `videos_output/` - результаты
  - [ ] Файл `queue_db/tasks.json` - статус задач

- [ ] Предоставить инструкции:
  - [ ] Как загрузить видео
  - [ ] Как проверить статус
  - [ ] Как скачать результат

## 🚀 Готово!

### Финальная проверка

- [ ] Сервер запущен и доступен
- [ ] API документация работает: `http://localhost:8000/docs`
- [ ] Тестовое видео обработано успешно
- [ ] Логи проверены и чисты
- [ ] Команда обучена использованию

### Команды для быстрого доступа

```bash
# Запуск сервера
python run_server.py

# Проверка здоровья
python client.py health

# Статистика
python client.py stats

# Просмотр логов
tail -f server_logs/server.log

# Список задач
python client.py list

# Помощь
python client.py --help
```

### Полезные ссылки

- 📚 [Полная документация](./SERVER_README.md)
- 🚀 [Быстрый старт](./QUICKSTART_SERVER.md)
- 🏗️ [Архитектура](./SERVER_ARCHITECTURE.md)
- 📖 [Примеры интеграции](./INTEGRATION_EXAMPLES.md)
- 🔧 [Production развертывание](./SERVER_DEPLOYMENT.md)

---

**Поздравляем! Ваш Media Cleaner Server готов к использованию!** 🎉

Для вопросов и проблем проверьте логи в `server_logs/` и документацию выше.

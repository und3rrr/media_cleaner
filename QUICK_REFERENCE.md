# 📌 БЫСТРЫЙ СПРАВОЧНИК (Quick Reference)

## 🚀 За 30 секунд

```bash
# 1. Установка зависимостей (одноразово)
pip install -r requirements.txt

# 2. Проверка окружения
python setup_check.py

# 3. Запуск программы
python media_cleaner.py
```

---

## 📂 Какой файл читать?

| Что вы хотите? | Читайте |
|---|---|
| **Как использовать** | → README.md |
| **Как это работает** | → ARCHITECTURE.md |
| **Как установить** | → INSTALLATION.md |
| **Что изменилось** | → CHANGELOG.md |
| **Примеры кода** | → examples.py |
| **Проверить окружение** | → `python setup_check.py` |

---

## ⚡ Самые частые команды

```bash
# Запуск программы
python media_cleaner.py

# Проверка зависимостей
python setup_check.py

# Установка из requirements
pip install -r requirements.txt

# Проверка версии Python
python --version

# Проверка GPU (NVIDIA)
python -c "import torch; print(torch.cuda.is_available())"
```

---

## 🎯 Типичный сценарий

### Для обработки видео:

1. **Поместите видео** в папку `media_cleaner/`
   ```
   c:\Users\User\Desktop\media_cleaner\video.mp4
   ```

2. **Запустите программу**:
   ```bash
   python media_cleaner.py
   ```

3. **Выберите видео** → Введите параметры → Ждите!

4. **Результат**: `video_protected.mp4` в той же папке

### Типичные параметры (быстрый выбор):

| Что обработать | Кадры | Уровень звука | Частота |
|---|---|---|---|
| Всё видео | 1 | слабый | каждый |
| Только часть | 2 (вводите диапазон) | средний | каждый 5 |
| Медленное видео | 1 | сильный | каждый 10 |

---

## 🔧 Редакторство конфига

Файл: `config.json`

Самые полезные параметры:
```json
{
  "video_processing": {
    "epsilon": 0.011,        // ↑ = сильнее эффект, но заметнее
    "num_eot_transforms": 2  // ↑ = лучше, но медленнее
  },
  "audio_processing": {
    "high_freq_base": 17000  // Частота неслышимого тона
  }
}
```

---

## 🐛 Частые ошибки

| Ошибка | Решение |
|---|---|
| `ModuleNotFoundError: torch` | `pip install -r requirements.txt` |
| `FileNotFoundError: ffmpeg.exe` | Проверить путь в `config.json` |
| `CUDA out of memory` | Использовать CPU или меньше кадров |
| `Видео не открывается` | Попробовать mp4 формат |

---

## 📊 Производительность

### На GPU (NVIDIA, 1080p, 30fps):
- ~100 кадров/сек = 10 сек видео за 1 сек обработки
- 1 минута видео ≈ 5-10 мин обработки

### На CPU (Intel i7, 1080p, 30fps):
- ~10 кадров/сек = 1 сек видео за 1 сек обработки
- 1 минута видео ≈ 60 мин обработки

### Совет для ускорения:
1. Используйте GPU если есть
2. Обрабатывайте каждый 5-й кадр (`every_n_frames=5`)
3. Уменьшайте разрешение видео

---

## 📋 Параметры объяснены

### Диапазон кадров

```
Вариант 1: Применить ко ВСЕМУ видео
→ Обрабатывает ВСЕ кадры (от 1 до последнего)

Вариант 2: Выбрать диапазон
→ Обрабатывает только кадры от A до B
→ Полезно для тестирования или защиты конкретной части
```

### Уровень маскировки звука

```
очень слабый (0.0020)
  ├─ Минимальный эффект на ASR
  ├─ Почти не слышно
  └─ Быстрая обработка

слабый (0.0035)
  ├─ Хороший баланс эффекта и слышимости
  ├─ Рекомендуется для большинства случаев
  └─ Нормальная скорость

средний (0.0050)
  ├─ Сильное воздействие на ASR
  ├─ Слышны микрошумы (малозаметны)
  └─ Немного медленнее

сильный (0.0080)
  ├─ Максимальное воздействие на ASR
  ├─ Слышны артефакты (заметно)
  └─ Медленнее всего
```

### Частота применения шума

```
Каждый кадр (every_n=1)
  ├─ Максимальный эффект
  ├─ Лучшее качество результата
  └─ Медленнее всего

Каждый 5-й кадр (every_n=5)
  ├─ Хороший баланс эффекта и скорости
  ├─ 20% кадров с шумом
  └─ Рекомендуется для быстрой обработки

Каждый 10-й кадр (every_n=10)
  ├─ Минимальный эффект
  ├─ 10% кадров с шумом
  └─ Самая быстрая обработка
```

---

## 🧪 Примеры использования

### Из Python кода:

```python
from media_cleaner import VideoProcessor, AudioProcessor

# Создание обработчиков
video = VideoProcessor()
audio = AudioProcessor()

# Обработка видео
temp_folder, noisy_count = video.process_video(
    "input.mp4", 
    start_frame=1, 
    end_frame=1000,
    every_n_frames=1
)

# Обработка аудио
audio.add_imperceptible_audio_noise("audio.wav", "audio_adv.wav", "слабый")
```

Больше примеров в `examples.py`!

---

## 📝 Логирование

Все события записываются в `imperceptible_protected_video.log`:

```
2026-01-12 10:23:45,123 - INFO - Используется устройство: cuda
2026-01-12 10:23:50,456 - INFO - Параметры видео: 1800 кадров @ 30.0fps
2026-01-12 10:24:30,012 - WARNING - Ошибка кадра 500, используется оригинал
2026-01-12 10:25:00,345 - ERROR - Критическая ошибка: ...
```

**Совет**: Смотрите лог при ошибках!

---

## ✅ Контрольный список перед запуском

- [ ] Python 3.8+
- [ ] `pip install -r requirements.txt` ✓
- [ ] Видео в папке `media_cleaner/`
- [ ] `python setup_check.py` ✓ везде
- [ ] FFmpeg работает

---

## 🎓 Где учиться?

1. **README.md** — Полный гайд пользователя
2. **ARCHITECTURE.md** — Технические подробности
3. **examples.py** — Готовые примеры
4. **INSTALLATION.md** — Пошаговая установка
5. **CHANGELOG.md** — История изменений

---

## 💬 Вопросы и ответы

**Q: Можно ли обрабатывать части видео?**
A: Да! Выберите вариант 2 при запуске и укажите диапазон кадров.

**Q: Что будет со звуком если его нет?**
A: Программа создаст молчаливое видео. Это нормально.

**Q: Можно ли менять параметры в коде?**
A: Да, но проще менять в `config.json`.

**Q: На сколько процентов видно изменения?**
A: Для человека — практически никак (<1% яркости). Для ИИ — катастрофа!

**Q: Сколько времени обработка?**
A: На GPU: 1 минута видео ≈ 5-10 мин. На CPU: ≈ 60 мин.

---

## 🚀 Готово!

```bash
python media_cleaner.py
```

Всё просто! 🎥✨

---

*Последнее обновление: 12 января 2026*

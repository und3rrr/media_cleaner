# Полная инструкция установки и первого запуска

## Требования к системе

- **ОС**: Windows 10+ (работает и на Linux/macOS, но путь к FFmpeg другой)
- **Python**: 3.8+ (проверить: `python --version`)
- **RAM**: Минимум 4 GB (рекомендуется 8 GB)
- **Диск**: Свободное место в 2-3 раза больше размера видео
- **GPU**: NVIDIA CUDA (опционально, но значительно ускоряет)

---

## Шаг 1: Проверка Python

### Windows
```bash
python --version
python -c "import sys; print(sys.executable)"
```

Если Python не найден:
1. Скачайте с [python.org](https://www.python.org)
2. При установке: **отметьте "Add Python to PATH"**
3. Перезагрузитесь
4. Проверьте ещё раз

### Linux/macOS
```bash
python3 --version
python3 -c "import sys; print(sys.executable)"
```

---

## Шаг 2: Клонирование/Копирование проекта

### Вариант 1: Git (если у вас установлен git)
```bash
git clone <repo-url>
cd media_cleaner
```

### Вариант 2: Файлы уже на вашем компьютере
```bash
# Всё уже в папке c:\Users\User\Desktop\media_cleaner
cd c:\Users\User\Desktop\media_cleaner
```

---

## Шаг 3: Проверка структуры папки

Убедитесь, что есть:
```
media_cleaner/
├── media_cleaner.py
├── config.json
├── requirements.txt
├── examples.py
├── setup_check.py
├── README.md
├── ARCHITECTURE.md
├── CHANGELOG.md
├── SUMMARY.md
└── ffmpeg/ffmpeg/bin/ffmpeg.exe
```

Если чего-то не хватает, скачайте файлы с GitHub.

---

## Шаг 4: Установка зависимостей

### Вариант 1: Через requirements.txt (РЕКОМЕНДУЕТСЯ)

```bash
pip install -r requirements.txt
```

Это установит:
- torch (PyTorch для GPU/CPU)
- torchvision (модели ResNet)
- opencv-python (cv2)
- librosa (обработка звука)
- soundfile (сохранение WAV)
- tqdm (progress bar)
- и другие

### Вариант 2: Ручная установка (если первый способ не сработал)

```bash
pip install torch torchvision torchaudio
pip install opencv-python Pillow numpy
pip install librosa soundfile
pip install tqdm
```

### Вариант 3: Использование conda (если установлен Anaconda)

```bash
conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia
conda install opencv librosa soundfile tqdm
```

---

## Шаг 5: Проверка окружения

```bash
python setup_check.py
```

Программа проверит:
- ✅ Все ли зависимости установлены
- ✅ FFmpeg доступен
- ✅ Есть ли видео-файлы в папке

Вывод должен быть:
```
[1/3] Проверка зависимостей Python...
✓ Все зависимости установлены

[2/3] Проверка FFmpeg...
✓ FFmpeg доступен

[3/3] Поиск видеофайлов...
✓ Найдено видеофайлов: 0

===============================================================================
✓ ВСЁ ГОТОВО! Можно запустить программу:

   python media_cleaner.py
```

---

## Шаг 6: Проверка CUDA (опционально, но рекомендуется)

Если у вас есть NVIDIA GPU:

```bash
python -c "import torch; print(torch.cuda.is_available())"
```

- Если `True` → GPU будет использоваться (БЫСТРО!)
- Если `False` → будет использоваться CPU (медленно, но работает)

Установка CUDA драйверов (если нужно):
1. [NVIDIA CUDA Toolkit](https://developer.nvidia.com/cuda-downloads)
2. [cuDNN](https://developer.nvidia.com/cudnn)

---

## Шаг 7: Первый запуск

### Подготовка видео

1. Скопируйте видео-файл в папку `media_cleaner/`
   ```
   c:\Users\User\Desktop\media_cleaner\video.mp4
   ```

2. Поддерживаемые форматы:
   - `.mp4` (рекомендуется)
   - `.mov`
   - `.avi`
   - `.mkv`
   - `.webm`

### Запуск программы

```bash
python media_cleaner.py
```

Вывод:
```
╔════════════════════════════════════════════════════════════════╗
║ Imperceptible Protected Video Generator v2.0                   ║
║ Максимально незаметная защита от CV и ASR                      ║
╚════════════════════════════════════════════════════════════════╝

Доступные видео в папке:
----------------------------------------------------------------------
 1 | video.mp4
----------------------------------------------------------------------

Выберите номер видео (0 = выход): 1
```

---

## Шаг 8: Выбор параметров

### 1. Диапазон кадров
```
=== Настройки обработки ===
Всего кадров в видео: 1800
1. Применить ко всему видео
2. Выбрать диапазон (от... до...)
Ваш выбор (1 или 2): 1
```

Выберите `1` для первого запуска.

### 2. Уровень маскировки звука
```
Уровень маскировки звука:
1. очень слабый
2. слабый
3. средний
4. сильный
Выберите уровень (1-4): 2
```

Для первого запуска выберите `2` (слабый).

### 3. Частота применения шума
```
Частота применения шума:
1. Каждый кадр
2. Каждый 5-й кадр
3. Каждый 10-й кадр
Выберите (1-3): 1
```

Выберите `1` (каждый кадр) для лучшего результата.

---

## Шаг 9: Обработка (и ждите!)

```
[1/4] Обработка видео...
Обработка видео |████████░░| 45% [450/1000, 00:30<00:40, 15 fps]

[2/4] Извлечение аудио...
Аудио извлечено → video_audio_orig.wav

[3/4] Маскировка аудио...
Аудио-маскировка уровня 'слабый' добавлена → video_audio_adv.wav

[4/4] Сборка финального видео...
✓ Видео собрано → video_protected.mp4

Очистка временных файлов...
Удалена временная папка: video_temp_adv
Временные файлы очищены

======================================================================
✓ ГОТОВО! Обработано кадров: 1800, с шумом: 1800
✓ Конечный файл: video_protected.mp4
======================================================================
```

---

## Шаг 10: Проверка результата

```bash
# Выходной файл находится в той же папке:
ls -la video_protected.mp4
```

Откройте `video_protected.mp4` в плеере и проверьте:
- ✅ Видео воспроизводится
- ✅ Визуально похоже на оригинал
- ✅ Звук похож на оригинал
- ✅ Файл имеет разумный размер

---

## 🔧 Решение проблем

### Проблема: "ModuleNotFoundError: No module named 'torch'"

**Решение**:
```bash
pip install torch torchvision torchaudio
```

### Проблема: "FFmpeg не найден"

**Решение**:
1. Проверить путь в `config.json`:
   ```json
   "ffmpeg_path": "C:\\users\\user\\desktop\\media_cleaner\\ffmpeg\\ffmpeg\\bin\\ffmpeg.exe"
   ```
2. Убедитесь, что путь существует
3. Если FFmpeg отдельно:
   ```bash
   # Скачайте FFmpeg с ffmpeg.org
   # Распакуйте и обновите путь в config.json
   ```

### Проблема: "CUDA out of memory"

**Решение**:
1. Используйте CPU (медленнее, но работает):
   ```python
   # В media_cleaner.py измените:
   DEVICE = torch.device("cpu")  # вместо "cuda"
   ```
2. Обработайте меньше кадров за раз
3. Уменьшите epsilon в config.json

### Проблема: "Видео не открывается"

**Решение**:
1. Проверить формат видео (mp4, mov, avi, mkv, webm)
2. Проверить кодеки (H.264 должен быть)
3. Попробовать переконвертировать:
   ```bash
   ffmpeg -i input.avi -c:v libx264 -c:a aac output.mp4
   ```

### Проблема: "Python не найден" (Windows)

**Решение**:
1. Переинсталлируйте Python с python.org
2. **Отметьте**: "Add Python to PATH"
3. Перезагрузитесь
4. Откройте новый терминал и попробуйте ещё раз

### Проблема: "Нет свободного диска"

**Решение**:
1. Освободите место (нужно 2-3x размер видео)
2. Используйте `every_n_frames=10` вместо `1` (меньше временных файлов)

---

## 🎯 Быстрая проверка (2 минуты)

```bash
# 1. Проверка окружения (30 сек)
python setup_check.py

# 2. Запуск программы (если есть видео в папке)
python media_cleaner.py

# 3. Выбрать видео и обработать с дефолт параметрами (1-2 мин)
```

---

## 📚 Дополнительные материалы

После первого запуска читайте:
1. **README.md** — Как использовать программу
2. **ARCHITECTURE.md** — Как это работает (технические детали)
3. **examples.py** — Примеры программного использования

---

## ✅ Контрольный список готовности

- [ ] Python 3.8+ установлен
- [ ] `pip install -r requirements.txt` выполнен
- [ ] `python setup_check.py` вывел ✓ везде
- [ ] Видео-файл скопирован в папку
- [ ] `python media_cleaner.py` запускается
- [ ] Программа обрабатывает видео успешно
- [ ] Выходной файл воспроизводится

---

## 🚀 Готово!

Если всё прошло успешно, вы можете начать использовать программу! 

```bash
python media_cleaner.py
```

Enjoy! 🎥✨

---

## 💬 Помощь и поддержка

- Смотрите `imperceptible_protected_video.log` при ошибках
- Проверьте README.md и ARCHITECTURE.md для деталей
- Примеры в examples.py показывают различные использования

---

*Последнее обновление: 12 января 2026*

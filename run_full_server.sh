#!/bin/bash
# ═══════════════════════════════════════════════════════════════════════
# Media Cleaner - Запуск полного сервера (Linux/Debian)
# ═══════════════════════════════════════════════════════════════════════

echo ""
echo "╔════════════════════════════════════════════════════════════════╗"
echo "║          MEDIA CLEANER - Запуск сервера + интерфейс          ║"
echo "╚════════════════════════════════════════════════════════════════╝"
echo ""

# Проверить Python
if ! command -v python3 &> /dev/null; then
    echo "❌ Python3 не установлен!"
    echo "Установите: sudo apt install python3 python3-pip"
    exit 1
fi

# Проверить FFmpeg
if ! command -v ffmpeg &> /dev/null; then
    echo "⚠️  FFmpeg не установлен"
    echo "Установите: sudo apt install ffmpeg"
fi

echo "[1/3] 📦 Проверка зависимостей..."
pip3 install -r requirements.txt

echo ""
echo "[2/3] 🚀 Запуск API сервера на http://127.0.0.1:8000..."
python3 run_server.py &
API_PID=$!

# Ждать пока сервер запустится
sleep 3

echo ""
echo "[3/3] 🌐 Запуск веб интерфейса на http://127.0.0.1:5000..."
python3 -c "import webbrowser; webbrowser.open('http://127.0.0.1:5000')" 2>/dev/null &
python3 web_interface.py &
WEB_PID=$!

echo ""
echo "✅ Сервер запущен!"
echo ""
echo "🌐 Откройте в браузере: http://127.0.0.1:5000"
echo "📡 API доступен на: http://127.0.0.1:8000"
echo ""
echo "Для остановки нажмите Ctrl+C"

# Ждать сигнала на выход
wait

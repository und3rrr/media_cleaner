# Media Cleaner Server v2.0 - Полная документация

> Серверное приложение для обработки видео с добавлением невидимого шума, защищающего от CV и ASR моделей

## 🚀 Быстрый старт

**Если вы в спешке, прочитайте это первым:**

```bash
# 1. Установить зависимости
pip install -r requirements.txt

# 2. Проверить конфигурацию
python server_config.py

# 3. Запустить сервер
python run_server.py

# 4. Открыть в браузере
http://localhost:8000/docs
```

👉 **Подробнее:** [QUICKSTART_SERVER.md](./QUICKSTART_SERVER.md)

## 📚 Полная документация

### Для администраторов (развертывание сервера)

| Документ | Описание |
|----------|---------|
| [SERVER_DEPLOYMENT.md](./SERVER_DEPLOYMENT.md) | **Полное руководство по развертыванию** |
| | - Установка на Linux/Windows |
| | - Конфигурация для production |
| | - Nginx и Gunicorn |
| | - Systemd сервис |
| | - Мониторинг и логирование |

### Для разработчиков (архитектура и интеграция)

| Документ | Описание |
|----------|---------|
| [SERVER_ARCHITECTURE.md](./SERVER_ARCHITECTURE.md) | **Архитектура системы** |
| | - Компоненты и их взаимодействие |
| | - Поток обработки задач |
| | - Структура БД |
| | - Масштабируемость |
| [INTEGRATION_EXAMPLES.md](./INTEGRATION_EXAMPLES.md) | **Примеры интеграции** |
| | - Python примеры |
| | - Flask веб приложение |
| | - HTML + JavaScript интерфейс |
| | - Docker контейнер |
| | - Webhook уведомления |

### REST API документация

| Эндпоинт | Метод | Описание |
|----------|-------|---------|
| `/upload` | POST | Загрузить видео для обработки |
| `/task/{task_id}` | GET | Получить статус задачи |
| `/tasks` | GET | Список задач (с фильтрацией) |
| `/download/{task_id}` | GET | Скачать обработанное видео |
| `/cancel/{task_id}` | POST | Отменить задачу |
| `/stats` | GET | Статистика сервера |
| `/health` | GET | Проверка здоровья |
| `/cleanup` | POST | Удаление старых задач |

👉 **Интерактивная документация:** `http://your-server:8000/docs` (Swagger UI)

## 🏗️ Структура проекта

```
media_cleaner/
├── 🌐 СЕРВЕР
│   ├── server_config.py         ← Конфигурация
│   ├── server_app.py            ← REST API
│   ├── server_video_worker.py   ← Обработка видео
│   ├── queue_processor.py       ← Очередь задач
│   ├── run_server.py            ← Запуск сервера
│   └── client.py                ← Python клиент
│
├── 📁 ДАННЫЕ (создаются автоматически)
│   ├── videos_input/            ← Входящие видео
│   ├── videos_output/           ← Обработанные видео
│   ├── videos_temp/             ← Временные файлы
│   ├── server_logs/             ← Логи
│   └── queue_db/                ← БД задач
│
└── 📚 ДОКУМЕНТАЦИЯ
    ├── QUICKSTART_SERVER.md     ← Начните отсюда!
    ├── SERVER_DEPLOYMENT.md     ← Production развертывание
    ├── SERVER_ARCHITECTURE.md   ← Техническая архитектура
    └── INTEGRATION_EXAMPLES.md  ← Примеры кода
```

## 📖 Руководства по сценариям

### Сценарий 1: Запуск собственного сервера для команды

1. Прочитайте [QUICKSTART_SERVER.md](./QUICKSTART_SERVER.md)
2. Запустите `python run_server.py`
3. Используйте [client.py](./client.py) для загрузки видео

### Сценарий 2: Развертывание на production сервере

1. Прочитайте [SERVER_DEPLOYMENT.md](./SERVER_DEPLOYMENT.md)
2. Установите системный сервис (systemd)
3. Настройте Nginx как reverse proxy
4. Используйте Gunicorn для масштабирования

### Сценарий 3: Интеграция в веб приложение

1. Посмотрите [INTEGRATION_EXAMPLES.md](./INTEGRATION_EXAMPLES.md)
2. Используйте Flask/Django пример
3. Добавьте HTML форму для загрузки
4. Отслеживайте статус через JavaScript

### Сценарий 4: Автоматическая обработка видео

1. Используйте Python клиент: [client.py](./client.py)
2. Создайте скрипт для batch обработки
3. Запланируйте с помощью cron

### Сценарий 5: Docker контейнер для облака

1. Прочитайте Docker пример в [INTEGRATION_EXAMPLES.md](./INTEGRATION_EXAMPLES.md)
2. Создайте Dockerfile
3. Разверните на AWS/GCP/Heroku

## 🔧 Конфигурация

### Основные параметры

```python
# server_config.py
SERVER_CONFIG = {
    "host": "0.0.0.0",              # Слушать на всех интерфейсах
    "port": 8000,                   # REST API порт
    
    "max_video_size_gb": 2,         # Максимальный размер видео
    "max_concurrent_tasks": 3,      # Одновременные обработки
    "task_timeout_hours": 24,       # Таймаут задачи
    
    "default_video_epsilon": 0.120, # Параметры по умолчанию
    "default_audio_level": "слабый",
}
```

### Запуск с параметрами

```bash
# Локальный тест (1 worker)
python run_server.py --port 8000 --workers 1

# Production (4 workers, отключить debug)
python run_server.py --host 192.168.1.100 --workers 4

# С отладкой
python run_server.py --debug
```

## 💻 Примеры использования

### Python клиент (рекомендуется)

```python
from client import MediaCleanerClient

client = MediaCleanerClient("http://localhost:8000")

# Загрузить видео
result = client.upload_video("video.mp4", epsilon=0.15)
task_id = result['task_id']

# Ждать завершения
task = client.wait_for_completion(task_id)

# Скачать результат
if task['status'] == 'completed':
    client.download_result(task_id, output_path="./results")
```

### Командная строка

```bash
# Загрузить и ждать (с автоскачиванием)
python client.py upload video.mp4 --wait --download ./results

# Проверить статус
python client.py status <task_id>

# Список задач
python client.py list --user user_001
```

### curl (любая платформа)

```bash
# Загрузить
curl -X POST "http://localhost:8000/upload" \
  -F "file=@video.mp4" \
  -F "epsilon=0.15"

# Проверить статус
curl http://localhost:8000/task/<task_id>

# Скачать
curl http://localhost:8000/download/<task_id> -o result.mp4
```

### Web интерфейс

Просто откройте в браузере: **http://localhost:8000/docs**

## 📊 Статистика и мониторинг

### Проверить здоровье сервера

```bash
python client.py health
```

### Получить статистику

```bash
python client.py stats
```

Ответ:
```json
{
  "queue": {
    "total": 15,
    "pending": 3,
    "processing": 2,
    "completed": 10,
    "failed": 0
  }
}
```

### Просмотр логов

```bash
tail -f server_logs/server.log      # Основной лог
tail -f server_logs/queue.log       # Лог обработки видео
```

## 🔐 Безопасность

### Уникализация файлов

Каждому видео назначается уникальный ID:
- Входной файл: `a1b2c3d4_original_name.mp4`
- Выходной файл: `a1b2c3d4_original_name_protected.mp4`

Это предотвращает конфликты при одновременной обработке.

### Изоляция пользователей (опционально)

Добавьте проверку `user_id` при скачивании:

```python
@app.get("/download/{task_id}")
async def download(task_id: str, user_id: str = Query(...)):
    task = processing_queue.get_task(task_id)
    
    if task.user_id != user_id:
        raise HTTPException(status_code=403, detail="Access denied")
    
    # Скачать файл
```

## 🚨 Трубешутинг

| Проблема | Решение |
|----------|---------|
| Сервер не запускается | `python server_config.py` для проверки конфигурации |
| Видео не обрабатывается | Проверьте `server_logs/queue.log` |
| Медленная обработка | Увеличьте `--workers` при запуске |
| Ошибка FFmpeg | Проверьте путь в `server_config.py` |
| Занят порт 8000 | `python run_server.py --port 8001` |

## 📈 Масштабируемость

### Одиночный сервер

```bash
python run_server.py --workers 4 --port 8000
```

### Несколько серверов (load balancing)

```
            ┌─────────────────┐
            │   Nginx (порт 80)│
            └────────┬────────┘
                     │
        ┌────────────┼────────────┐
        ↓            ↓            ↓
   Server 1    Server 2    Server 3
   (8000)      (8000)      (8000)
   
# Каждый server: python run_server.py --port 8000
```

## 📦 Зависимости

Основные:
- `torch`, `torchvision` - для GPU обработки видео
- `opencv-python` - обработка кадров
- `librosa`, `soundfile` - обработка аудио
- `fastapi`, `uvicorn` - REST API
- `requests` - HTTP клиент

Установка:
```bash
pip install -r requirements.txt
```

## 🤝 Поддержка и обратная связь

- **Документация**: см. файлы `*.md` в папке проекта
- **Логи**: `server_logs/server.log` и `server_logs/queue.log`
- **API документация**: `http://your-server:8000/docs`

## 📄 Лицензирование

Это расширение основного проекта Media Cleaner v2.0

## 📝 История версий

### v2.0 (текущая)
- ✅ REST API на FastAPI
- ✅ Очередь обработки с фоновыми потоками
- ✅ Управление задачами с уникальными ID
- ✅ Python клиент для автоматизации
- ✅ Полная документация и примеры интеграции
- ✅ Поддержка batch обработки
- ✅ Мониторинг и логирование

---

**Начните с [QUICKSTART_SERVER.md](./QUICKSTART_SERVER.md) для быстрого введения!** 🚀

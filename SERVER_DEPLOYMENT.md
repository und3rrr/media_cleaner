# Развертывание Media Cleaner на сервере

## Обзор архитектуры

Серверная часть Media Cleaner состоит из следующих компонентов:

```
┌─────────────────────────────────────────────────────────────┐
│                     REST API Сервер                         │
│                      (FastAPI/Uvicorn)                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  📁 videos_input/   → Входящие видео от пользователей     │
│  📁 videos_output/  → Обработанные видео                   │
│  📁 videos_temp/    → Временные файлы обработки            │
│  📁 server_logs/    → Логи сервера                         │
│  📁 queue_db/       → База данных задач (JSON)             │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│              Очередь обработки (Background Tasks)            │
│                    (Threading Workers)                       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Обработчик 1: Обработка видео в фоновом режиме           │
│  Обработчик 2: Параллельная обработка                      │
│  Обработчик 3: и т.д. (кол-во зависит от конфига)        │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## Установка и конфигурация

### 1. Предварительные требования

```bash
# Python 3.8+
python --version

# Установка зависимостей
pip install fastapi uvicorn python-multipart requests opencv-python torch torchvision
```

### 2. Структура папок

При запуске сервер автоматически создаст следующую структуру:

```
media_cleaner/
├── videos_input/      # Папка для входящих видео
├── videos_output/     # Папка с обработанными видео
├── videos_temp/       # Временные файлы (автоматически удаляются)
├── server_logs/       # Логи сервера
├── queue_db/          # База данных задач (tasks.json)
│
├── server_config.py   # Конфигурация сервера
├── queue_processor.py # Управление очередью
├── server_app.py      # REST API
├── server_video_worker.py # Обработчик видео
├── run_server.py      # Скрипт запуска
└── client.py          # Клиент для API
```

### 3. Конфигурация сервера

Отредактируйте `server_config.py`:

```python
SERVER_CONFIG = {
    "host": "0.0.0.0",              # Слушать на всех интерфейсах
    "port": 8000,                   # Порт REST API
    
    "max_video_size_gb": 2,         # Максимальный размер видео
    "max_concurrent_tasks": 3,      # Одновременные обработки
    
    "default_video_epsilon": 0.120, # Параметры по умолчанию
    "default_audio_level": "слабый",
}
```

### 4. Запуск сервера

#### Локальный запуск (для тестирования):

```bash
python run_server.py
```

#### На удаленном сервере (production):

```bash
# С указанием адреса сервера
python run_server.py --host 192.168.1.100 --port 8000 --workers 3

# С отладкой
python run_server.py --debug
```

#### Параметры командной строки:

- `--host` - IP адрес для прослушивания
- `--port` - Порт (по умолчанию 8000)
- `--workers` - Количество обработчиков видео
- `--debug` - Включить режим отладки

## REST API Эндпоинты

### 1. Загрузка видео

```http
POST /upload
```

**Параметры:**
- `file` - Видео-файл (mp4, mov, avi, mkv, webm)
- `epsilon` - Сила шума (0.04-0.20, по умолчанию 0.12)
- `video_strength` - Множитель (1.0-2.0, по умолчанию 1.0)
- `audio_level` - Маскировка аудио (None/"слабый"/"средний"/"сильный")
- `every_n_frames` - Каждый N-й кадр (1-30, по умолчанию 10)
- `user_id` - ID пользователя (опционально)
- `notes` - Заметки (опционально)

**Пример с curl:**

```bash
curl -X POST "http://localhost:8000/upload" \
  -F "file=@video.mp4" \
  -F "epsilon=0.15" \
  -F "audio_level=средний" \
  -F "user_id=user_001"
```

**Ответ:**

```json
{
  "status": "success",
  "task_id": "a1b2c3d4",
  "message": "Видео загружено и добавлено в очередь",
  "task": {
    "task_id": "a1b2c3d4",
    "status": "pending",
    "status_text": "⏳ Ожидает обработки",
    "created_at": "2026-02-11T14:37:00",
    "progress": 0.0,
    "input_video": "a1b2c3d4_video.mp4"
  }
}
```

### 2. Получить статус задачи

```http
GET /task/{task_id}
```

**Пример:**

```bash
curl http://localhost:8000/task/a1b2c3d4
```

**Ответ:**

```json
{
  "status": "success",
  "task": {
    "task_id": "a1b2c3d4",
    "status": "processing",
    "status_text": "⚙️  Обрабатывается",
    "progress": 45.5,
    "input_video": "a1b2c3d4_video.mp4"
  }
}
```

### 3. Список задач

```http
GET /tasks
```

**Параметры (опционально):**
- `user_id` - Фильтр по пользователю
- `status` - Фильтр по статусу (pending/processing/completed/failed)
- `limit` - Максимум результатов (по умолчанию 50)

**Пример:**

```bash
# Все задачи пользователя
curl http://localhost:8000/tasks?user_id=user_001

# Завершённые задачи
curl http://localhost:8000/tasks?status=completed
```

### 4. Скачать обработанное видео

```http
GET /download/{task_id}
```

**Пример:**

```bash
curl http://localhost:8000/download/a1b2c3d4 -o result_video.mp4
```

### 5. Отменить задачу

```http
POST /cancel/{task_id}
```

**Пример:**

```bash
curl -X POST http://localhost:8000/cancel/a1b2c3d4
```

### 6. Статистика сервера

```http
GET /stats
```

**Ответ:**

```json
{
  "status": "success",
  "queue": {
    "total": 15,
    "pending": 5,
    "processing": 2,
    "completed": 8,
    "failed": 0
  }
}
```

### 7. Проверка здоровья

```http
GET /health
```

### 8. Очистка старых задач

```http
POST /cleanup?days=7
```

## Использование Python клиента

### Установка зависимостей:

```bash
pip install requests
```

### Примеры использования:

```python
from client import MediaCleanerClient

# Создание клиента
client = MediaCleanerClient("http://localhost:8000")

# 1. Загрузка видео
result = client.upload_video(
    "my_video.mp4",
    epsilon=0.15,
    audio_level="средний",
    user_id="user_001"
)
task_id = result['task_id']
print(f"Задача создана: {task_id}")

# 2. Ожидание завершения
task = client.wait_for_completion(task_id, check_interval=5, timeout=3600)

# 3. Скачивание результата
if task['status'] == 'completed':
    output_file = client.download_result(task_id, output_path="./results")
    print(f"Видео сохранено в: {output_file}")

# 4. Получение статуса
status = client.get_task_status(task_id)
print(f"Статус: {status['status_text']}")
print(f"Прогресс: {status['progress']}%")

# 5. Список задач пользователя
tasks = client.list_tasks(user_id="user_001")
for task in tasks:
    print(f"- {task['task_id']}: {task['status_text']}")

# 6. Отмена задачи
client.cancel_task(task_id)
```

### Использование командной строки:

```bash
# Загрузка и ожидание (с автоматическим скачиванием)
python client.py upload my_video.mp4 --wait --download ./results

# Проверка статуса
python client.py status a1b2c3d4

# Ожидание завершения
python client.py status a1b2c3d4 --wait --download ./results

# Список задач пользователя
python client.py list --user user_001

# Статистика
python client.py stats

# Отмена задачи
python client.py cancel a1b2c3d4
```

## Развертывание на production сервере

### Использование Gunicorn + Nginx

#### 1. Установка зависимостей:

```bash
pip install gunicorn
```

#### 2. Systemd сервис (Linux):

Создайте `/etc/systemd/system/media-cleaner.service`:

```ini
[Unit]
Description=Media Cleaner Video Server
After=network.target

[Service]
Type=notify
User=video-processor
WorkingDirectory=/home/video-processor/media_cleaner
Environment="PATH=/home/video-processor/media_cleaner/venv/bin"
ExecStart=/home/video-processor/media_cleaner/venv/bin/gunicorn \
    --workers 4 \
    --worker-class uvicorn.workers.UvicornWorker \
    --bind 127.0.0.1:8000 \
    server_app:app

Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

#### 3. Запуск сервиса:

```bash
sudo systemctl start media-cleaner
sudo systemctl enable media-cleaner
sudo systemctl status media-cleaner
```

#### 4. Конфигурация Nginx:

```nginx
server {
    listen 80;
    server_name your-server.com;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        
        # Увеличиваем таймауты для больших видео
        proxy_connect_timeout 60s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
        
        # Увеличиваем лимит размера загрузки
        client_max_body_size 2G;
    }
}
```

## Мониторинг и логирование

### Просмотр логов:

```bash
# Основной лог
tail -f server_logs/server.log

# Лог обработки видео
tail -f server_logs/queue.log

# Реальный мониторинг
tail -f server_logs/*.log | grep ERROR
```

### Проверка статус очереди:

```bash
# Python скрипт для мониторинга
python -c "
from queue_processor import processing_queue
stats = processing_queue.get_statistics()
print('📊 Статистика:')
for key, val in stats.items():
    print(f'  {key}: {val}')
"
```

## Трубешутинг

### Проблема: Сервер не запускается

```bash
# Проверить конфигурацию
python server_config.py

# Проверить порт
netstat -tuln | grep 8000

# Проверить права доступа на папки
ls -la videos_*/
```

### Проблема: Видео не обрабатывается

```bash
# Проверить статус задачи
python client.py status <task_id>

# Проверить логи
grep ERROR server_logs/queue.log

# Проверить входной файл
ls -lh videos_input/
```

### Проблема: Низкая производительность

```python
# Увеличить количество обработчиков
python run_server.py --workers 5

# Проверить использование памяти
ps aux | grep python
```

## Безопасность

### Рекомендации:

1. **Аутентификация**: Добавить API ключи в конфиг
2. **HTTPS**: Использовать SSL сертификаты
3. **Файерволл**: Ограничить доступ к портам
4. **Очистка**: Регулярно удалять старые файлы
5. **Лимиты**: Установить максимальный размер видео и количество одновременных задач

### Пример добавления API ключа:

```python
# server_app.py
from fastapi.security import APIKeyHeader

api_key_header = APIKeyHeader(name="X-API-Key")

@app.post("/upload")
async def upload_video(api_key: str = Depends(api_key_header), ...):
    if api_key not in VALID_API_KEYS:
        raise HTTPException(status_code=403, detail="Invalid API key")
```

## Интеграция с другими системами

### Webhook уведомления (опционально):

```python
import httpx

async def notify_webhook(task_id: str, status: str, webhook_url: str):
    async with httpx.AsyncClient() as client:
        await client.post(webhook_url, json={
            "task_id": task_id,
            "status": status,
            "timestamp": datetime.now().isoformat()
        })
```

## Поддержка и обновления

- Документация: [Полная документация API](./API_DOCS.md)
- Примеры: [Примеры интеграции](./EXAMPLES.md)
- Issues: Сообщайте об ошибках в логах сервера
